# ─── Prompt Commerce Gateway — Supabase Environment ──────────────────────────
#
# Copy this file to .env when deploying to Supabase / production.
#
# Where to find your Supabase URLs:
#   Supabase Dashboard → Settings → Database → Connection string
#
#   DATABASE_URL      = "Transaction" mode (port 6543) — used at runtime
#   DIRECT_DATABASE_URL = "Session" mode (port 5432) — used by Prisma migrations
#
# ─── Database ─────────────────────────────────────────────────────────────────

# Runtime URL — Supabase Transaction pooler (pgBouncer), port 6543
# Format:  postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.[YOUR-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1

# Migration URL — Supabase Direct connection, port 5432
# Format:  postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres
DIRECT_DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-REF].supabase.co:5432/postgres

# ─── Gateway ──────────────────────────────────────────────────────────────────
GATEWAY_PORT=3002

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING
JWT_EXPIRES_IN=7d

# ─── Admin account ────────────────────────────────────────────────────────────
ADMIN_USERNAME=admin
ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ─── File uploads ─────────────────────────────────────────────────────────────
UPLOAD_DIR=./uploads
